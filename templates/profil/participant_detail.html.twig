{% extends 'base.html.twig' %}


{% block body %}
    <h1> Info Organisateur </h1>
    <br>
    <div>
    <ul>
        <img src="{{ asset('/images/products/'~user.imageName) }}" alt="Image de profil">
        <li>Prénom : {{user.prenom}}</li>
        <li>Nom : {{user.nom}}</li>
        <li>Téléphone : {{user.telephone  }}</li>
        <li>Email : {{ user.email }}</li>
        <li>Site de rattachement : {{ user.siteEni.nom }}</li>
    </ul>
    </div>


{% endblock %}