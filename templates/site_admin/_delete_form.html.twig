<form method="post" action="{{ path('app_site_admin_delete', {'id': site.id}) }}" onsubmit="return confirm('êtes-vous sûr de vouloir supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ site.id) }}">
    <button class="btn btn-sm m-1">Supprimer</button>
</form>
