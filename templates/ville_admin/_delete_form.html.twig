<form method="post" action="{{ path('app_ville_admin_delete', {'id': ville.id}) }}" onsubmit="return confirm('êtes-vous sûr de vouloir supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ville.id) }}">
    <button class="btn btn-sm m-1">Supprimer</button>
</form>
